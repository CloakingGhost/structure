훨씬더 짧은 시간복잡도
평균 O(NlogN)
최악 O(N^2): 이미 정렬된 배열인 경우
- 참조 지역성(locality of reference)
- 한번 결정된 pivot 값은 이후의 연산에서 제외

동일한 배열내에서 원소들이 자리를 이동
인접 데이터 사이에서 이동 발생
때문에 처음 배열에 접근할 때만 실제 메모리에서 데이터를 가져오고
이후로는 캐시로 배열에 접근하여 훨씬 빠른 접근이 가능

메모리 접근이 아닌 캐시 접근
MergeSort 진행 시 보조 배열을 생성했기 때문에 메인 메모리에서 값을 확인해야한다
QuickSort


추가적인 메모리 공간 사용 X
불안정 정렬
분할 정복 알고리즘

최악의 시나리오는 막기위해 Median of Three 방법이 있다